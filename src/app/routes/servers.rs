use leptos::*;

#[component]
pub fn Servers(cx: Scope) -> impl IntoView {
    view! { cx,
        <div>
            <h1>{"Servers"}</h1>
            <p>{"This is the servers page."}</p>
        </div>
    }
}
